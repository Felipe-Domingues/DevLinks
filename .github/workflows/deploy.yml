name: 🚀 Pipeline Principal via workflow externo

on:
  push:
    branches:
      - staging

permissions:
  actions: read
  contents: read

jobs:
  call-main-workflow:
    name: 🔁 Executando workflow principal
    uses: Felipe-Domingues/workflows-base/.github/workflows/main.yml@main
    secrets: inherit
